name: Auto-private timer

on:
  workflow_dispatch:
    inputs:
      expire_in_minutes:
        description: 'Set/overwrite the auto-private timer (minutes). Leave blank to skip.'
        required: false
        default: ''
      check_now:
        description: 'Run the expiry check immediately (does not change the timer)'
        required: false
        default: false
        type: boolean
  schedule:
    - cron: '*/10 * * * *'

jobs:
  timer:
    runs-on: ubuntu-latest
    steps:
      - name: Start the timer
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.expire_in_minutes != ''
        env:
          GH_TOKEN: ${{ secrets.PAGES_TOKEN }}
          REPO: ${{ github.repository }}
          MINUTES: ${{ github.event.inputs.expire_in_minutes }}
        run: |
          expiry=$(( $(date +%s) + MINUTES * 60 ))
          gh variable set AUTO_PRIVATE_AT --body "$expiry" -R "$REPO"
          echo "Timer set: repo becomes private at $(date -u -d @$expiry) UTC"

      - name: Check expiry and make private
        if: github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.event.inputs.check_now == 'true')
        env:
          GH_TOKEN: ${{ secrets.PAGES_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          expiry=$(gh variable get AUTO_PRIVATE_AT -R "$REPO" 2>/dev/null || echo "")
          if [ -z "$expiry" ]; then
            echo "No timer set."
            exit 0
          fi
          now=$(date +%s)
          if [ "$now" -ge "$expiry" ]; then
            echo "Timer expired. Making repo private..."
            gh repo edit "$REPO" --visibility private --accept-visibility-change-consequences
            gh variable delete AUTO_PRIVATE_AT -R "$REPO" 2>/dev/null || true
            echo "Done: repo is now private."
          else
            mins=$(( (expiry - now) / 60 ))
            echo "Not yet: private in about ${mins} minute(s)."
          fi